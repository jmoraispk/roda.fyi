# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
coverage
dev-dist
.vite

# Local env / secrets — committed example only.
.env
.env.local
.env.*.local

# Supabase CLI temp / local-dev artefacts.
supabase/.temp/
supabase/.branches/
archive/berimbau-music/supabase/.temp/
archive/berimbau-music/supabase/.branches/

# Vercel CLI link state (project + org IDs).
.vercel/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# Archived berimbau-music large audio scratch (kept on disk, not in git).
archive/berimbau-music/scratch/*.f32
archive/berimbau-music/scratch/*.wav
archive/berimbau-music/scratch/*.ogg

# Mocap pipeline (Python): virtualenv, model weights, extracted frames.
tools/mocap/.venv/
tools/mocap/__pycache__/
tools/mocap/frames/
*.pt
*.onnx
*.task

# Downloaded reference video (heavy; re-fetchable). Keep the provenance
# manifest + the captured keyframes/QA, drop the raw video + overlays.
tools/mocap/clips/*.webm
tools/mocap/clips/*.ogv
tools/mocap/clips/*.ogg
tools/mocap/clips/*.mp4
tools/mocap/clips/*.mkv
tools/mocap/clips/*.m4v
tools/mocap/clips/*.part
tools/mocap/out/*.mp4
tools/mocap/out/*.webm
# Transient search debug (regenerated by fetch_youtube.py each run).
tools/mocap/clips/candidates.json

# Source-zip + scratch extraction.
roda.zip
tmp_roda/

# QA screenshots + python caches.
tools/shots/
__pycache__/

# Own capture footage (heavy; never commit) + large per-frame tracks.
videos/*.mp4
videos/*.mov
videos/*.MOV
tools/mocap/out/*.track.json
# Transient frame extracts from exploration.
_explore/
